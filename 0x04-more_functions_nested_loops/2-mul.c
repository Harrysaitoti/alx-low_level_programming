#include "main.h"
#include <stdio.h>
/**
 * _mul - mutltiply 2 integers
 * @a: multplied with b integer
 * b: multiplied with a integer
 * Return - 0 Always
 */
int mul(int a, int b)
{
	return (a * b);
}
