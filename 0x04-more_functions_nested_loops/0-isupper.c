#include "main.h"
#include <stdio.h>
/**
 * _isupper - checks for uppercase
 * @c: int character to be checked
 * Return: to 1 if c, is uppercase 0 otherwise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
